bundle:
  name: bundle_test

include:
  - resources/*.yml

targets:

  development:
    mode: development
    default: true
    workspace:
      host: https://dbc-d6e0c605-f7d8.cloud.databricks.com/
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
